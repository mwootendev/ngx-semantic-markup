import { MicrodataValueDirective } from './microdata-value.directive';

describe('MicrodataValueDirective', () => {
  it('should create an instance', () => {
    const directive = new MicrodataValueDirective();
    expect(directive).toBeTruthy();
  });
});
