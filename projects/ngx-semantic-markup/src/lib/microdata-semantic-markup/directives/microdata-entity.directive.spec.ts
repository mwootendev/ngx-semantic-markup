import { MicrodataEntityDirective } from './microdata-entity.directive';

describe('MicrodataEntityDirective', () => {
  it('should create an instance', () => {
    const directive = new MicrodataEntityDirective();
    expect(directive).toBeTruthy();
  });
});
