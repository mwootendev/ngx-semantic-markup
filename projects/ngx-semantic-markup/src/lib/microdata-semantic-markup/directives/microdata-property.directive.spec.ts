import { MicrodataPropertyDirective } from './microdata-property.directive';

describe('MicrodataPropertyDirective', () => {
  it('should create an instance', () => {
    const directive = new MicrodataPropertyDirective();
    expect(directive).toBeTruthy();
  });
});
