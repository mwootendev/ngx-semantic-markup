import { RdfaEntityDirective } from './rdfa-entity.directive';

describe('RdfaEntityDirective', () => {
  it('should create an instance', () => {
    const directive = new RdfaEntityDirective();
    expect(directive).toBeTruthy();
  });
});
