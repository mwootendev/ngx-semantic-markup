import { RdfaValueDirective } from './rdfa-value.directive';

describe('RdfaValueDirective', () => {
  it('should create an instance', () => {
    const directive = new RdfaValueDirective();
    expect(directive).toBeTruthy();
  });
});
