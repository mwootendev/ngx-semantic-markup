import { RdfaPropertyDirective } from './rdfa-property.directive';

describe('RdfaPropertyDirective', () => {
  it('should create an instance', () => {
    const directive = new RdfaPropertyDirective();
    expect(directive).toBeTruthy();
  });
});
